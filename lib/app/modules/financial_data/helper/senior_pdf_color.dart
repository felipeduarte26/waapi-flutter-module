import 'package:pdf/pdf.dart';

mixin SeniorPdfColor implements PdfColors {
  /// This color is represented by the hexadecimal value #ECEEF1.
  static const PdfColor secondaryColor100 = PdfColor.fromInt(0xFFECEEF1);

  /// This color is represented by the hexadecimal value #D8DEE2.
  static const PdfColor secondaryColor200 = PdfColor.fromInt(0xFFD8DEE2);

  /// This color is represented by the hexadecimal value #0E866A.
  static const PdfColor primaryColor600 = PdfColor.fromInt(0xFF0E866A);

  /// This color is represented by the hexadecimal value #D43A45.
  static const PdfColor manchesterColorRed500 = PdfColor.fromInt(0xFFD43A45);

  /// This color is represented by the hexadecimal value #080808.
  static const PdfColor pureBlack = PdfColor.fromInt(0xFF080808);

  /// This color is represented by the hexadecimal value #FFFFFF.
  static const PdfColor pureWhite = PdfColor.fromInt(0xFFFFFFFF);

  /// This color is represented by the hexadecimal value #0E866A.
  static const PdfColor primaryColor = PdfColor.fromInt(0xFF0E866A);

  /// This color is represented by the hexadecimal value #D0EEE7.
  static const PdfColor primaryColor100 = PdfColor.fromInt(0xFFD0EEE7);

  /// This color is represented by the hexadecimal value #A0DCCE.
  static const PdfColor primaryColor200 = PdfColor.fromInt(0xFFA0DCCE);

  /// This color is represented by the hexadecimal value #71CBB6.
  static const PdfColor primaryColor300 = PdfColor.fromInt(0xFF71CBB6);

  /// This color is represented by the hexadecimal value #41B99D.
  static const PdfColor primaryColor400 = PdfColor.fromInt(0xFF41B99D);

  /// This color is represented by the hexadecimal value #12A885.
  static const PdfColor primaryColor500 = PdfColor.fromInt(0xFF12A885);
}
