/// The possible types for the list items. It can be [ItemType.emphasis], [ItemType.emphasisBold], [ItemType.neutral] and
/// [ItemType.neutralBold].
enum ItemType {
  emphasis,
  emphasisBold,
  neutral,
  neutralBold,
}
