import 'package:senior_platform_authentication/senior_platform_authentication.dart';

const SamlProvider samlProviderMock = SamlProvider(
  url: 'url',
);

const SamlProviderModel samlProviderModelMock = SamlProviderModel(
  url: 'url',
);

const String samlProviderJson = '''
  {
    "url": "url"
  }
''';
