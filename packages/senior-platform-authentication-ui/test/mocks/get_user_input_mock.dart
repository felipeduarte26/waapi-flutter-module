import 'package:senior_platform_authentication/src/domain/entities/entities.dart';

const GetUserInput getUserInputMock = GetUserInput(
  accessToken: 'accessToken',
  includePhoto: true,
  username: 'username',
);
