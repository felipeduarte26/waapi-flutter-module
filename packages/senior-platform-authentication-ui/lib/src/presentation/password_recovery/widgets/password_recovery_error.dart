import 'package:flutter/material.dart';

import '../../../core/widgets/generic_error.dart';

class PasswordRecoveryError extends StatelessWidget {
  const PasswordRecoveryError({super.key});

  @override
  Widget build(BuildContext context) {
    return const GenericError();
  }
}
