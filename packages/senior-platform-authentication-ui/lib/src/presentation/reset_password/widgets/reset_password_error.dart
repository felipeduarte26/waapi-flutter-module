import 'package:flutter/material.dart';

import '../../../core/widgets/generic_error.dart';

class ResetPasswordError extends StatelessWidget {
  const ResetPasswordError({super.key});

  @override
  Widget build(BuildContext context) {
    return const GenericError();
  }
}
